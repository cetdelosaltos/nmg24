/* import { SupabaseClient, createClient } from '@supabase/supabase-js';
import { supdb } from '../variables';
const conas: SupabaseClient = createClient(supdb.supabaseUrl, supdb.supabaseKey);

export default conas;
 */